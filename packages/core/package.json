{
	"name": "@shop/core",
	"version": "1.0.0",
	"private": true,
	"main": "dist/index.js",
	"typings": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "rimraf dist && swc src -d dist && tsc --build"
	},
	"dependencies": {
		"argon2": "0.30.3",
		"fast-jwt": "2.2.1"
	}
}
